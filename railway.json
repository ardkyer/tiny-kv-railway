{
    "$schema": "https://railway.app/railway.schema.json",
    "build": { "nixpacks": { "plan": { "providers": ["python"] } } },
    "deploy": {
      "startCommand": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}",
      "healthcheckPath": "/health",
      "healthcheckTimeout": 100
    }
  }
  